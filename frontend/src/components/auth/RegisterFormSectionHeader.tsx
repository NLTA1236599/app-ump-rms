/** In-card header for register tab (`register-component-analysis.md` §5). */
export function RegisterFormSectionHeader() {
  return (
    <div className="mb-1">
      <h2 className="text-[16px] font-semibold leading-snug text-[#1a1a1a]">Đăng ký tài khoản</h2>
      <p className="mt-1 text-[13px] font-normal leading-relaxed text-[#6b7280]">
        Tạo tài khoản mới — cần xác nhận qua email trước khi đăng nhập
      </p>
    </div>
  );
}
