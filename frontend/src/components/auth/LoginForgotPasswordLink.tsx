/** Analysis §5.3 — wire to real route when forgot-password exists. */
export function LoginForgotPasswordLink() {
  return (
    <a
      href="#forgot-password"
      className="shrink-0 text-[12px] font-medium text-[#6b7280] underline decoration-transparent underline-offset-2 transition-colors hover:text-[#1a6ec2] hover:decoration-[#1a6ec2] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#1a6ec2] focus-visible:ring-offset-1"
    >
      Quên mật khẩu?
    </a>
  );
}
