/** Fixed dashboard sidebar width — keep in sync with `DashboardSidebar` classes. */
export const CHROME_SIDEBAR_WIDTH_PX = 220;

/** Offset for main column when sidebar is `fixed` (see `MainLayout`). Must match `CHROME_SIDEBAR_WIDTH_PX`. */
export const chromeMainOffsetClass = 'ml-[220px]';
