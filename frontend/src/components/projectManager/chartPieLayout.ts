/** Shared pie/donut layout so charts scale on laptop and PC viewports. */

export const PIE_INNER_PCT = '52%';
export const PIE_OUTER_PCT = '82%';

export type ChartLegendItem = {
  name: string;
  color: string;
  detail?: string;
};
