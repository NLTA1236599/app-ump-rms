import type { ResearchProject } from './types.js';

/** Returns the initial project dataset (empty — data is added via import or manual entry). */
export function createDemoProjects(): ResearchProject[] {
  return [];
}
